[pytest]
testpaths = tests
# -p no:xonsh: this dev machine's shared venv ships the xonsh pytest plugin,
# which tries to open an interactive console and crashes headless runs. Blocking
# a plugin that isn't installed (e.g. in CI) is a harmless no-op.
addopts = -q -p no:xonsh
